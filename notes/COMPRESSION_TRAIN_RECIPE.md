# Compression train recipe card (stub)

**Status:** **T0–T4 GREEN (proxy) · TRAIN-LOCKED** — packing + \(k\)-sweep + SVD + BitDistill + scale U/N proven on toys; do **not** start real model training. Fill/lock this card for a train rung only after explicit quality gates (T4≠unlock).  
**Gate:** [`COMPRESSION_TRAIN_READY.md`](COMPRESSION_TRAIN_READY.md) · North star: [`COMPRESSION_NORTH_STAR.md`](COMPRESSION_NORTH_STAR.md)  
**Hypotheses:** [`COMPRESSION_NOVEL.md`](COMPRESSION_NOVEL.md) (ALBERT-BitMoE primary · LoRA-Hive control · SVD-TieStack · BitDistill)  
**T0 needle:** `OVERSEER_COMPRESSION_T0_PACK_2026_09_05` — ALBERT \(S\)=83.33/98.04; Hive \(S\)=99.80/99.98 @ \(N_L\)=1e5/1e6; pack≈\(U/8\); **quality N/A**  
**T0 pack-cache needle:** `OVERSEER_COMPRESSION_T0_PACK_CACHE_2026_09_05` — durable dump/load/`--ensure-default-pack` → `notes/compression_artifacts/t0_pack_report.json`; **TRAIN still LOCKED**  
**Dual SoT (pack+logit) gate:** `python3 scripts/compression_t0_pack.py --dual-sot-check` — requires `t0_pack_report.json` + `t3_teacher_logit_bank.json` both green (`dual_sot_ok`); **still TRAIN-LOCKED** (green Dual SoT ≠ unlock)  
**Dual SoT probe keep-alive:** `python3 scripts/compression_t0_pack.py --probe-keep-alive --json` — undersize expand → ensure heals → `probe_ok` + dump `notes/compression_artifacts/probe_keep_alive_report.json` (`OVERSEER_COMPRESSION_DUAL_SOT_PROBE_KEEP_ALIVE_2026_09_08` · Lane-U cache `OVERSEER_COMPRESSION_DUAL_SOT_PROBE_CACHE_2026_09_08`); `--check-probe-report` reloads report + live expand/Dual SoT + **RAM-safe stamp-refresh** (`OVERSEER_COMPRESSION_PROBE_STAMP_REFRESH_2026_09_08` — re-dumps ts/hub-unlock/`ram_soft_skip` from **live** MemAvailable only — clears sticky soft after RAM recovers; stamps `ram_floor_kb`; no undersize mutation); **still TRAIN-LOCKED**
**Dual SoT stamp-refresh:** `python3 scripts/compression_t0_pack.py --check-dual-sot-report --json` — on `dual_sot_ok`, re-dumps `dual_sot_report.json` with fresh `ts` + **live** `ram_soft_skip` (`OVERSEER_COMPRESSION_DUAL_SOT_STAMP_REFRESH_2026_09_08` · `OVERSEER_COMPRESSION_DUAL_SOT_RAM_SOFT_REFRESH_2026_09_08`; falsifier `test_check_dual_sot_report_stamp_refresh`); clears sticky soft without full `--ensure-dual-sot`; CLI `dual_sot_report_path` stays under peer worktree via `_redirect_hub_artifact_path`; **still TRAIN-LOCKED**
**Lane-U post-unittest heal:** `python3 scripts/compression_t0_pack.py --heal-lane-u-sot --json` — runs Dual SoT + probe stamp-refresh (`OVERSEER_COMPRESSION_LANE_U_SOT_HEAL_2026_09_08`; falsifier `test_heal_lane_u_sot_clears_sticky_soft`); clears sticky soft after soft-skip unit tests without heavy ensure; `heal_ok` + `train_unlocked=false`; **still TRAIN-LOCKED**
**Hub unlock ignore:** hub/sibling `notes/compression_artifacts/train_unlock.json` may stamp `train_unlocked=true` — Dual SoT / probe detect + ignore (`OVERSEER_COMPRESSION_HUB_UNLOCK_IGNORE_2026_09_08` → `hub_unlock_ignored=true`, `train_unlocked=false`); steward-revoked unlock JSON (`train_unlocked=false`) still stamps `hub_unlock_file_needle` from the file (`OVERSEER_COMPRESSION_HUB_UNLOCK_FILE_NEEDLE_REVOKED_2026_09_08` — lose-needle-after-revoke was Dual SoT theater); peer recipe stub remains SoT until explicit TRAIN-LOCK card fill
**Worktree root bind:** bare `import compression_t0_pack` with hub `scripts/` on `sys.path` while cwd is `.worktrees/peer-N` must dump Dual SoT/probe/pack to the worktree (`OVERSEER_COMPRESSION_WORKTREE_ROOT_BIND_2026_09_08` → `_resolve_repo_root` + `_redirect_hub_artifact_path` on dump **and** load; durable stamps `worktree_root_bind_needle` + `artifact_root`; falsifier `test_worktree_root_bind_prefers_cwd`; ensure/check pin `dual_sot_report_path` via `_redirect_hub_artifact_path` before load/dump); hub intentional cwd dumps stay on hub; **TRAIN still LOCKED**
**Expand-path coerce:** Dual SoT/probe report ``expand_path`` junk (e.g. ``z``) or missing file → `coerce_expand_path` falls back to Lane-U EXPANDED (`OVERSEER_COMPRESSION_EXPAND_PATH_COERCE_2026_09_08` + `test_coerce_expand_path_refuses_junk`); durable Dual SoT/probe dumps stamp ``expand_path_coerce_needle`` (CLI-only was theater — `dump_*` + stamp-refresh surface); **TRAIN still LOCKED**
**Dual SoT Lane-U cache:** `python3 scripts/compression_t0_pack.py --ensure-dual-sot` — refresh pack+logit **+ logit-expand sidecar** (floor 200) + dump `notes/compression_artifacts/dual_sot_report.json` (`OVERSEER_COMPRESSION_DUAL_SOT_CACHE_2026_09_07`); `--check-dual-sot-report` reloads report (refuse `expand_ok≠true`) + live pack/logit + live expand `check_expanded` keep-alive + **RAM-safe Dual SoT stamp-refresh** (`OVERSEER_COMPRESSION_DUAL_SOT_STAMP_REFRESH_2026_09_08` — re-dumps ts/hub-unlock/`ram_soft_skip` from **live** MemAvailable only — clears sticky soft after RAM recovers; stamps `ram_floor_kb` + durable `stamp_refreshed=true`; no heavy ensure); Dual SoT RAM-soft-refresh needle `OVERSEER_COMPRESSION_DUAL_SOT_RAM_SOFT_REFRESH_2026_09_08`; `--dual-sot-check` stays pack+canonical; **RAM soft-skip** `OVERSEER_COMPRESSION_DUAL_SOT_RAM_SOFT_SKIP_2026_09_08` (MemAvailable&lt;`ENSURE_DUAL_SOT_RAM_FLOOR_KB` 8GiB → check-only; `COMPRESSION_ENSURE_DUAL_FORCE=1` for full heal); soft-skip dump persists `ram_soft_skip`+`ram_soft_needle`+`mem_available_kb` + hub unlock file needle; `--check-probe-report` stamp-refresh persists durable `stamp_refreshed=true` (before dump); falsifiers `test_ensure_dual_sot_ram_soft_skip` + `test_check_dual_sot_report_live_expand_keep_alive` + `test_ensure_dual_sot_heals_undersized_expand` + `test_dual_sot_ignores_hub_train_unlock` + `test_check_probe_report_stamp_refresh` + `test_check_dual_sot_report_stamp_refresh` + `test_worktree_root_bind_prefers_cwd` (snip_limit=16→floor / stale-report refuse / soft-skip no ensure_expanded / hub unlock ignored / durable stamp_refreshed / Dual SoT sticky-soft clear / worktree-root-bind); refuse prune/unlock; **still TRAIN-LOCKED** · check-CLI prefers existing report (RAM-safe 2026-09-08)  
**T1 needle:** `OVERSEER_COMPRESSION_T1_SHARE_2026_09_05` — \(k\in\{2,4,8,16\}\) ALBERT+Distill-Fold \(U\downarrow\); recon MSE↑; **cliff before \(S\approx100\)**; `data_prune=false`; quality=**proxy**  
**T1 share-cache needle:** `OVERSEER_COMPRESSION_T1_SHARE_CACHE_2026_09_05` — durable dump/load/`--ensure-default-ablation` → `notes/compression_artifacts/t1_share_ablation_report.json`; refuse prune/unlock; **TRAIN still LOCKED**  
**T2 needle:** `OVERSEER_COMPRESSION_T2_SVD_LORA_2026_09_05` — SVD+LoRA heldout MSE↓ under \(U\) budget; quality=**proxy**  
**T2 svd-cache needle:** `OVERSEER_COMPRESSION_T2_SVD_LORA_CACHE_2026_09_05` — durable dump/load/`--ensure-default-report` → `notes/compression_artifacts/t2_svd_lora_report.json`; refuse prune/unlock; **TRAIN still LOCKED**  

**T3 needle:** `OVERSEER_COMPRESSION_T3_BITDISTILL_2026_09_05` — teacher logit bank → SVD student; heldout KD≤ε; **no prune**; quality=**proxy**  
**T3 cache needle:** `OVERSEER_COMPRESSION_T3_LOGIT_CACHE_2026_09_05` — durable full-corpus bank dump/load (Lane U); `--check-cache` roundtrip; **not** a train unlock  
**T3 logit-cache:** `OVERSEER_COMPRESSION_T3_LOGIT_CACHE_2026_09_05` — durable dump/load bank roundtrip KD-stable (`--write-bank`/`--read-bank`/`--check-cache`/`--ensure-default-bank`); canonical path `notes/compression_artifacts/t3_teacher_logit_bank.json`; **TRAIN still LOCKED**  
**T3 logit-expand needle:** `OVERSEER_COMPRESSION_LOGIT_EXPAND_2026_09_07` — `scripts/compression_logit_expand.py` `--ensure`/`--check` → `notes/compression_artifacts/t3_teacher_logit_bank_expanded.json` (sibling beside canonical; parent=T3 BitDistill; seed=mean-pooled `bank` @ `d_out`, not `xs`/`d_in`; durable floor `DEFAULT_EXPAND_SNIPS=200`); refuse prune/unlock/xs-as-logits; **TRAIN still LOCKED** · `--dual-sot-check` pack+canonical; `--ensure-dual-sot` refreshes expand  
**T4 needle:** `OVERSEER_COMPRESSION_T4_SCALE_2026_09_05` — \(N_L\in\{10^6,10^7,10^8\}\) ALBERT+Hive U/N ±20% (max |rel−1|≈1.94%); pack util smoke; quality=**proxy**  
**T4 scale-cache needle:** `OVERSEER_COMPRESSION_T4_SCALE_CACHE_2026_09_05` — durable dump/load/`--ensure-default-report` → `notes/compression_artifacts/t4_scale_report.json`; refuse prune/unlock; **TRAIN still LOCKED**

| Field | Stub value |
|-------|------------|
| **Stack primary** | **ALBERT-BitMoE** — shared 1-bit expert FFN body across layers + small per-layer LoRA |
| **Stack polish** | **SVD-TieStack + LoRA** — tied truncated-SVD init under same \(U\) budget (T2 winner on toys) |
| **Distill** | **BitDistill** — teacher logit bank → body-shared student (T3); freeze readouts; full corpus |
| **Stack control** | **LoRA-Hive** — shared 1-bit hive + per-expert LoRA deltas |
| **Bitwidth** | Same **1-bit** packed baseline on logical and unique views (ternary/binary BitNet-class OK) |
| **\(S\) target** | Unique-param \(S = N_{\mathrm{logic}}/U \approx \mathbf{100}\) at 1B→10M; T0 bar \(S\ge50\) **met** on toys |
| **Distill teacher** | Independent-layer (or denser) expert stack → shared-body student; BitDistill / MiniLLM-class logits (+ optional hidden align). Distill = quality path **not** byte compressor (**C150**) |
| **Data policy** | **No data / example / token pruning.** Full corpus only. Weight sparsity only with explicit pack+index byte math |
| **Eval bar** | T0–T4 (done): pack/\(U\)/\(k\)-cliff/SVD/BitDistill KD + scale U/N ±20% proxies on toys. Never claim measured 1B→10M until quality gates + ledger + recipe TRAIN-LOCK |
| **Stop criteria** | Fail if unique/pack miss by >2×; fail if KD gap only closes via data prune → redesign. Do not jump to 1B on packing-only evidence |
| **Dual SoT (TRAIN-LOCK)** | Both green before any real train unlock: `t0_pack_report.json` (`--ensure-default-pack` / `--dual-sot-check` / `--ensure-dual-sot`) **and** `t3_teacher_logit_bank.json` (`--ensure-default-bank`). CLI: `python3 scripts/compression_t0_pack.py --ensure-dual-sot --json` → `dual_sot_ok=true` · `train_unlocked=false`. Dual SoT ≠ unlock. |
| **Research-speed refs** | [`RESEARCH_SPEED_TRAINING.md`](RESEARCH_SPEED_TRAINING.md) **S01–S32** + Lane U (CPU synthetic T0, unittest gate, OA/Hyperband, **teacher-logit cache GREEN** via T3 dump/load + `--ensure-default-bank` → `notes/compression_artifacts/t3_teacher_logit_bank.json`) |

**Honesty locks:** B1–B5 / Lane-C stacks = **hypothesis** on quality until measured (**C146/C152**). SSD/Zamba = serve util only (**C148/C149**). FP16→1-bit 16× ≠ north star (**C147**).

_Stub 2026-09-05 · fact_checker pristine audit. TRAIN-LOCKED until explicit recipe lock (T0–T4 toys ≠ unlock). T4 scale filed 2026-09-05._

**Recipe Lock Steward (2026-09-08T11:30Z · `OVERSEER_COMPRESSION_RECIPE_LOCK_REJECT_2026_09_08`):** unlock **REJECTED** — Status remains **TRAIN-LOCKED** (`recipe_locked=false`); hub `train_unlock.json` revoked to `train_unlocked=false`; arch drift=0 (ALBERT-BitMoE + LoRA / NVFP4 / `data_prune=false`); Dual SoT green ≠ unlock. Verdict: `notes/compression_artifacts/recipe_lock_verdict.json`. Do **not** treat prior `train_unlocked=true` stamp as approved TRAIN-LOCK card fill.
