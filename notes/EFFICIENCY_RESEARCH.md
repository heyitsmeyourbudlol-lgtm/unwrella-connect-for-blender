# Efficiency research

_Updated 2026-09-08 15:26:30_ · **Efficiency Research Agent**

> **Focus:** Per-worker yield, hot paths, latency, queue discipline, pre-dispatch — findings feed `notes/RESEARCH_SYNC.md`, `automation_improve`, and peer orchestrator prompts.

## This cycle

- probed 1 finding(s); **1 implemented** — CLEAR_DELAYS_LITE_STATE + WAKE_NO_COLD_IMPROVE (closes L128 remiss + stale CTX/_log_tail residual as already-MET)

## Open findings


## Agent notes

- **2026-09-08 (peer-coding / efficiency_researcher L135)** — Completes L128 Next residual (`clear_autonomy_delays` cold `peer_transcript` + `_wake_loops` cold `automation_improve`) + closes RESEARCH_SYNC CTX/_log_tail residual as already-MET. Evidence: L134 Next claimed ctx-skip / `_log_tail` read_text — hub already has `SCAN_LIVE_ERROR_HITS_CTX_SKIP_DISK` (ctx MISS **0.023ms** vs disk **0.835ms**, ~**36×**; OLD read_text **214.8ms**) and `_log_tail` seek EOF (**0.035ms**); green verify-cooldown never calls clear. Real remiss: `clear_autonomy_delays` always imported tx (**~13.47ms**) + `_wake_loops` imported improve. Landed hub+peer-coding+peer-1+hub-protect+golden: `_load_loop_state_lite` + save-only tx import + signal-only wake; needles `CLEAR_DELAYS_LITE_STATE_NO_TX_IMPORT_2026_09_08` + `CLEAR_DELAYS_WAKE_NO_COLD_IMPROVE_IMPORT_2026_09_08`; unittest `test_clear_autonomy_delays_clean_no_tx_no_improve_import`; EXPECTED `0fc1060e…` / test `d4363a2e…`. Measure: clean clear med **0.048ms** tx/improve **False** (~**278×** vs **13.47ms**); mutate save path med **0.194ms** improve **False**. Metric: **latency**/error-adapt clear + Lane E. Paths: `scripts/peer_error_adapt.py`, `tests/test_peer_error_adapt.py`. Distinct from L134 TAIL_64K / L126 WAKE_PEER / L129 VERIFY_COOLDOWN. Next: none required on clear path; optional `peer_oversight` kevent still disk-scans without ctx (MISS ~0.8ms expected on log write).

- **2026-09-08 (peer-1 / efficiency_researcher L134)** — Completes L133 remiss (improve-loop append every tick → mtime memo often MISS) + Lane E ASN. Evidence: default `tail_text_lines` max_bytes=**256KiB**×4 ≈**0.99ms** MISS; live last-100 only **~9KB**; 64KiB parity True @48k+. Landed hub+peer-1+golden: `_SCAN_LIVE_ERROR_TAIL_BYTES=65536` + needle `SCAN_LIVE_ERROR_HITS_TAIL_64K_2026_09_08`; unittests assert 64k; EXPECTED peer `87d6b9d7…` hub `d93c5fd4…`. Measure: MISS med **0.791ms** (was ~0.99–1.6ms @256k); HIT **0.014ms**. Metric: **latency**/verify-cooldown remiss + Lane E. Paths: `scripts/peer_oversight_events.py`, `tests/test_peer_oversight_events.py`. Distinct from L133 MTIME_MEMO / L132 SEEK_TAIL. Next: skip disk tails when ctx supplies `peer_log_tail`/`improve_log_tail`, or fix `peer_command_builder._log_tail` still `read_text()[-n]`.

- **2026-09-08 (peer-1 / efficiency_researcher L133)** — Completes L132 Next residual (mtime-memo after SEEK_TAIL) + Lane E ASN. Evidence: green verify_ok cooldown still re-sought tails every tick (MISS **~1.5–3.1ms**) while log mtimes unchanged. Landed hub+peer-1: `_LIVE_SCAN_CACHE` + `_live_scan_mtime_key` + `clear_scan_live_error_hits_cache`; needle `SCAN_LIVE_ERROR_HITS_MTIME_MEMO_2026_09_08`; unittest `test_scan_live_error_hits_mtime_memo_skips_reseek`; EXPECTED peer `65647b75…` hub `e846680f…`. Measure: HIT med **0.014ms** (~**218×** vs MISS **3.06ms**; ~**16600×** vs OLD read_text **233.7ms**). Metric: **latency**/verify-cooldown remiss + Lane E. Paths: `scripts/peer_oversight_events.py`, `tests/test_peer_oversight_events.py`. Distinct from L132 SEEK_TAIL / L91 LOG_HAS_RECENT_ANY_MTIME_MEMO. Next: skip disk tails when ctx already supplies `peer_log_tail`/`improve_log_tail`.

- **2026-09-08 (peer-1 / efficiency_researcher L132)** — Completes RESEARCH_SYNC residual (`scan_live_error_hits` on green verify_ok cooldown) + Lane E ASN noop/compact. Evidence: `apply_mechanical` (`run_verify=False`) still called `scan_live_error_hits` when `last_cycle.verify_ok` ≠ False; body used `path.read_text()+splitlines()[-100]` on hub improve-loop **~61MB**. Landed hub+peer-1: `auto.tail_text_lines(path, 100)` seek-from-EOF; needle `SCAN_LIVE_ERROR_HITS_SEEK_TAIL_2026_09_08`; unittest `test_scan_live_error_hits_seek_tail_no_full_read` (`Path.read_text` not called); hub-protect+golden synced; EXPECTED peer `65647b75…` hub `e846680f…`. Measure: hub scan med **1.60ms** vs OLD read_text **218.5ms** (~**136×**). Metric: **latency**/verify-cooldown remiss + Lane E. Paths: `scripts/peer_oversight_events.py`, `tests/test_peer_oversight_events.py`. Distinct from L131 QUEUE_FP_LITE_DRY / L129 VERIFY_COOLDOWN lite state. Next: mtime-memo `scan_live_error_hits` when CONFIG_DIR log mtimes unchanged (~1.5ms→≪1ms) or skip disk tails when ctx already supplies `peer_log_tail`/`improve_log_tail`.

- **2026-09-08 (peer-1 / efficiency_researcher L131)** — Completes RESEARCH_SYNC DRY residual (lite fp into project_automation) + Lane E ASN. Evidence: three duplicate zlib adler+crc paths (`peer_transcript.queue_fingerprint`, `improve.queue_fingerprint_items`, `peer_loop._open_queue_content_fingerprint`); improve lite still optionally measured live. Landed hub+peer-1+hub-protect+golden: `auto.queue_fingerprint_items` + `auto.current_queue_fingerprint_lite(live=None)` SoT; thin wrappers; needle `QUEUE_FP_LITE_DRY_PROJECT_AUTOMATION_2026_09_08`; unittest `test_queue_fp_lite_dry_project_automation`. Measure: auto lite med **0.035ms**; parity **True**; EXPECTED hub `50177507…`. Metric: **latency**/DRY remiss + Lane E. Paths: `scripts/project_automation.py`, `scripts/peer_loop.py`, `scripts/automation_improve.py`, `scripts/peer_transcript.py`, `tests/test_automation.py`. Distinct from L130 COMPACT_SKIP_MET peer-1 / L129 VERIFY_COOLDOWN / L129 PRE_DISPATCH_LITE. Next: `peer_oversight_events.scan_live_error_hits` on green verify_ok cooldown (~1.1ms).

- **2026-09-08 (peer-1 / efficiency_researcher L130)** — Completes L126/L127 residual that L127 claimed hub+peer but only landed on **peer-coding** + Lane E ASN noop/compact. Evidence: peer-1 live open MET WQ/CTX **False** yet `compact_executable_queue` always imported `peer_worktree`+`peer_transcript` (cold **18.6ms** tx=wt=**True**; warm **~3.0ms**). Landed peer-1: `_MET_ALL_MARKERS` + open-marker gate + needle `COMPACT_SKIP_MET_TRANSCRIPT_NO_OPEN_2026_09_08`; unittest `test_compact_skips_met_transcript_when_no_open_met_markers`; EXPECTED `f78aeef5…` / test `19e0e36e…`. Measure: cold **18.6→4.4ms**; tx/wt **False**; AutomationQueueTests 15 OK. Metric: **latency**/compact + Lane E. Paths: `scripts/project_automation.py`, `tests/test_automation.py`. Distinct from L127 peer-coding land / L129 VERIFY_COOLDOWN / L128 NOOP_CLEAR. Next: `peer_oversight_events.scan_live_error_hits` on green verify_ok cooldown (~1.1ms) or `apply_mechanical` never_delay→load_state.

- **2026-09-08 (peer-1 / efficiency_researcher L129)** — Completes RESEARCH_SYNC pre-dispatch residual (post-L128 improve noop-clear) + Lane E ASN. Evidence: hub `_mechanical_pre_dispatch` always `import peer_transcript` when state exists then `current_queue_fingerprint` on noop; peer-1 **blind-cleared** noop (no fp gate). Measure: cold import+fp **~13.34ms** vs lite med **0.384ms** (~**35×**); parity **True** (`654414f7…`); noop=False path **0** tx import. Landed hub+peer-1: `_open_queue_content_fingerprint` + wire FP-aware clear; needle `PRE_DISPATCH_NOOP_CLEAR_LITE_FP_2026_09_08`; unittest `test_open_queue_content_fingerprint_parity_no_transcript`; EXPECTED hub `93b27288…`. Metric: **latency**/pre-dispatch + Lane E noop. Paths: `scripts/peer_loop.py`, `tests/test_noop_efficiency_heal.py`. Distinct from L128 improve `NOOP_CLEAR_FP_NO_TX_IMPORT` / L127 COMPACT_SKIP_MET. Next: `apply_mechanical` never_delay→load_state tx import (~L827) or DRY lite fp into `project_automation`.

- **2026-09-08 (peer-1 / efficiency_researcher L128)** — Completes L127 Next residual (pre-dispatch noop-clear cold `peer_transcript.current_queue_fingerprint`) + Lane E ASN noop/compact. Evidence: `apply_mechanical` noop-clear always `import peer_transcript` then full `current_queue_fingerprint` (load WQ+SIC+live+items); cold import+fp **~16.8ms** (import **~13.8** + fp **~3.0**). Landed hub+peer-1+hub-protect+golden: `queue_fingerprint_items` + `current_queue_fingerprint_lite` (auto-only zlib adler+crc parity) + wire noop-clear; needle `NOOP_CLEAR_FP_NO_TX_IMPORT_2026_09_08`; unittest `test_noop_clear_fp_no_tx_import`; EXPECTED `ff0607d2…`. Measure: lite med **0.449ms** tx_in=**False** match=**True** (~**37×**). Metric: **latency**/noop-break + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L127 COMPACT_SKIP_MET / L126 WAKE_PEER. Next: `apply_mechanical` `run_verify=False` still may import tx via `peer_error_adapt.never_delay_on_error`→`load_state` (~L827).

- **2026-09-08 (peer-coding / efficiency_researcher L127)** — Completes L126 Next residual (compact-queue remiss) + Lane E ASN. Evidence: live WQ open MET/daemon/wake markers all **False** but `compact_executable_queue` always `import peer_worktree` + `peer_transcript.load_state` + `resolve_satisfied_met_theater` ×2 (Done/[x] MET history still matched full-md early check). Measure: MET cold import+load **~13.1ms**; resolve_met×2 **~1.05ms**; remiss before **~7.1ms** tx_in=**True** → after unload-tx remiss med **~6.36ms** tx_in=wt_in=**False**; open-MET path still calls resolve (closes). Unittest `test_compact_skips_met_transcript_when_no_open_met_markers`. Metric: **latency**/compact-queue + pre-dispatch. Paths: `scripts/project_automation.py`, `tests/test_automation.py`. Distinct from L99 COMPACT_SKIP_DAEMON_SNAP / L126 WAKE_PEER. Next: pre-dispatch noop-clear still cold-imports `peer_transcript` for `current_queue_fingerprint` (~5.7ms+2.3ms) — lite WQ+SIC mtime fp (~0.019ms).

- **2026-09-08 (overseer / WAKE_PEER_NO_COLD_TEAM_TX_IMPORT)** — Landed L126. `wake_peer` touches `auto.CONFIG_DIR/peer-turn.signal` only; skips `team_wake_hint`/`team_status` and `peer_transcript` import. Unittest `test_wake_peer_no_cold_team_tx_import`. Measure: cold wake **~89ms → ~0.05ms**; team_in=tx_in=**False**. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Next: Soft Soft #100 tip-cover or next remiss probe.

- **2026-09-08 (peer-coding / efficiency_researcher L126)** — Completes L125 Next residual (`wake_peer` cold imports on noop-break / Soft Soft writeback wake) + Lane E ASN. Evidence: hub `wake_peer` always `import automation_team` → `team_wake_hint()` → `team_status(quick=True)` then `import peer_transcript` solely to touch `SIGNAL_PATH` (`auto.CONFIG_DIR / "peer-turn.signal"`). Measure (fresh subprocess, improve already loaded): cold wake **~86.6–91.7ms** (med **~89ms**) team_in=tx_in=**True**; hint-only (often **None**) med **~79.5ms**; lite touch via `auto.CONFIG_DIR` med **0.047ms** team/tx **not** in modules (~**1900×**). Warm-loaded still **~67ms** (hint body). Metric: **latency**/noop+queue_fp wake + Soft Soft writeback yield. Paths: `scripts/automation_improve.py` (`wake_peer` ~L1110). Distinct from L125 RANK_TEAM_STATUS_LITE / L123 RANK_KEY_NO_TEAM. Next: land `WAKE_PEER_NO_COLD_TEAM_TX_IMPORT` — touch signal without tx; skip or disk-lite hint (no `team_status` on wake).

- **2026-09-08 (peer-1 / efficiency_researcher L125)** — Completes L123 Next residual (hub `rank_team_opportunities` → full `team_status`) + Lane E. Evidence: hub rank_team paid `debrief.status_dict` **~51.7ms** (total **~90.6ms**/MISS) while peer already lite+cache; hub board used `build_board`. Landed hub: `RANK_TEAM_STATUS_LITE` + `RANK_TEAM_STATUS_CACHE` + gen-cache + `agents_board_slice`; 8 team unittests OK. Measure: rank_team **90.6→HIT 0.032ms**; rank_opportunities MISS **76→6.2ms**. Metric: **latency**/rank remiss + Lane E. Paths: `Automation/scripts/automation_team.py`, `Automation/scripts/peer_agent_board.py`, `tests/test_automation_team.py`. Next: `wake_peer` cold automation_team+peer_transcript.

- **2026-09-08 (peer-1 / efficiency_researcher L123-pack)** — Completes L123 land packaging under tip-cover race + Lane E ASN. Evidence: tip had RANK_KEY_NO_TEAM / COMPACT / RANK_MERGE but EXPECTED lagged and golden dual-merge until sync; trailing paren cleaned. Synced hub-protect+golden+EXPECTED `ad2bc7bd…`. Re-measure: OLD import+AttrError med **0.268ms** → NEW rank_key med **0.027ms**; team_in=**False**; 3 ImproveRankTests OK. Metric: **latency**. Paths: `Automation/scripts/automation_improve.py`, hub-protect, hub-protect-golden. Next: `wake_peer` cold automation_team+peer_transcript (RESEARCH_SYNC).

- **2026-09-08 (peer-1 / efficiency_researcher L123)** — Completes L122 Next residual (`_rank_input_key` cold-imports `automation_team`) + Lane E ASN noop/compact. Evidence: hub `automation_team` lacked `_team_status_input_key` → remiss `import` then AttributeError → `team_key=()` while after-pop paid **~1.4–1.65ms**; peer helper path mtimes match CONFIG_DIR disk mirror. Landed hub+peer-1+hub-protect+golden+EXPECTED: `_team_rank_mtime_fp` + wire rank key; unittest `test_team_rank_mtime_fp_no_team_import`. Measure: after-pop remiss **1.42→0.033ms** (~**43×**); `automation_team` **not** in modules; team_key len=**10**. Metric: **latency**/rank remiss + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L122 RANK_MERGE / L102 peer RANK_MERGE. Next: port hub `automation_team` generation cache (`team_status_for_rank`) if rank_team_opportunities still cold-pays full `team_status`.

- **2026-09-08 (peer-1 / efficiency_researcher L122)** — Completes L121 Next residual (hub RANK_MERGE / `_rank_input_key` dual import) + Lane E ASN noop/compact adjacent. Evidence: hub `_merge_research_opportunities` + `_rank_input_key` still `import peer_dual_research` on remiss while peer already RANK_MERGE_NO_DUAL_IMPORT; unittests present but helpers missing (`_dual_findings_mtime_fp` / `_dual_findings_to_opportunities` AttributeError path). Landed hub+hub-protect-golden+EXPECTED: disk JSON merge + mtime fp; ballast skip-gc when absent. Measure: cold merge **73.4→4.5ms** (HIT med **0.046ms**, dual_in=**False**); cold rank_key **9.5→1.65ms** (HIT med **0.014ms**); 4 ImproveRankTests OK. Metric: **latency**/rank remiss + Lane E. Paths: `Automation/scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L121 GATHER_ADAPT_AUDIT_GEN_TTL / L102 peer RANK_MERGE. Next: probe remaining improve remiss cold imports (automation_team in `_rank_input_key`) or compact theater if live snaps return.

- **2026-09-08 (peer-1 / efficiency_researcher L121)** — Completes L120/L121 enqueue residual (hub `GATHER_ADAPT_AUDIT_GEN_TTL`) + Lane E. Evidence: L120 dual-skip helpers landed but hub `gather_signals` still always ran live `adapt.run_audit` with `audit_cache_hit=False` → dual skip never fired on remiss; L121 note had enqueued the port — this cycle landed it. Hub: `_resolve_gather_audit` + `_gather_audit_witness` (HEAD-only) + `clear_gather_audit_cache` / stub `clear_gather_signals_cache`; wire gather; EXPECTED md5 `f727412b…`. Measure: audit MISS **11.36ms** → HIT **0.217ms** (~**52×**); gather remiss **110→7ms**; `test_gather_adapt_audit_generation_ttl_hit` + dual-skip unittests OK. Metric: **latency**/improve remiss + Lane E. Paths: `Automation/scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L120 DUAL_GATHER_SKIP helpers / L119 SKIP_POST_WAIT. Next: hub `_rank_input_key` + `_merge_research_opportunities` still import `peer_dual_research` on remiss (RANK_MERGE_NO_DUAL_IMPORT peer←hub).

- **2026-09-08 (peer-1 / efficiency_researcher L121-probe)** — Completes L120 Next residual probe under tip-cover noop (before land). Evidence: hub `FIND_LATEST_ROOT_FP_GENERATION` past-TTL gen HIT **0.089ms** scans=**0**; dual-skip helpers live but `_resolve_gather_audit` absent until L121 land above. Enqueued then landed same cycle. Metric: **latency**/gather remiss. Paths: `Automation/scripts/automation_improve.py`.

- **2026-09-08 (peer-1 / efficiency_researcher L120)** — Completes L119 Next residual (audit-hit dual skip) + Lane E. Evidence: hub `gather_signals` always `import peer_dual_research` + `run_research_cycle(force=True)` when `audit=True` even on cooldown; unittests for `_resolve_gather_research_sync` already present but helpers missing (AttributeError). Landed hub+hub-protect+golden: `DUAL_GATHER_COOLDOWN_DISK_BRIEF` + `DUAL_GATHER_SKIP_ON_AUDIT_HIT` helpers; wire gather; EXPECTED tip `3ee5b4d9…`. Measure: audit_hit skip med **0.009ms**; cooldown disk brief **0.04ms** (was force cycle **~280–315ms** + import); 4 dual-sync unittests OK. Metric: **latency**/improve gather remiss + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L119 SKIP_POST_WAIT / L118 MD_INDEX / peer-1 dual helpers already live. Next: find_latest root-fp generation HIT on hub API skew, or GATHER_ADAPT_AUDIT_GEN_TTL if hub still pays full run_audit every live_audit.

- **2026-09-08 (peer-1 / efficiency_researcher L119)** — Completes L117/L118 Next residual (skip post-wait if still paying) + Lane E ASN noop/compact. Evidence: hub `run_forever` still always re-ran `find_latest`+`measure` after every wait while peer-1 already had `should_skip_post_wait_*`; after wake sleep TTL cold remiss **~30.7–36.5ms** (med **~32.4ms**) vs skip gate **0.0024ms**. Landed hub+hub-protect+golden: helpers + timeout/fallback wire; unittests `test_should_skip_post_wait_transcript_rescan` + `POST_WAIT_FIND_LATEST_FORCE=False`; EXPECTED md5. Measure: timeout/fallback skips scan (**~32ms→0**/wake). Metric: **latency**/wake + Lane E. Paths: `scripts/peer_loop.py`, `tests/test_automation.py`. Distinct from L118 MD_INDEX / L117 CONTENT_FP / peer-1 skip already live. Next: find_latest root-fp generation HIT or audit-hit dual skip.

- **2026-09-08 (peer-1 / efficiency_researcher L118)** — Completes L117 Next residual (wire `queue_md_known_keys` into cold miss) + Lane E ASN noop/compact adjacent. Evidence: CONTENT_FP landed but cold miss still line-scanned via `_parse_done_item`/`_parse_work_item`; hub+peer unittests counting `queue_md_known_keys` saw scans=**0** (false green path); CREATIVE_BACKLOG content-fp×index. Landed hub+peer-1: cold miss `keys |= auto.queue_md_known_keys(md)` + needle `QUEUE_KNOWN_KEYS_MD_INDEX_2026_09_08`; EXPECTED md5 hub `1648f4f2…` peer `42a29f45…` test `74d54d4f…`. Measure: MISS med **~2.08–3.81ms**; GEN HIT **~0.005ms**; CONTENT_FP remiss **~0.015ms** scans=**0** (~**130×**); both unittests OK. Metric: **latency**/rank remiss + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L117 CONTENT_FP land / L116 SOFT_REFRESH. Next: find_latest root-fp generation HIT or audit-hit dual skip.

- **2026-09-08 (peer-1 / efficiency_researcher L117)** — Completes L116 Next residual (QUEUE_KNOWN_KEYS) + Lane E noop/compact adjacent (improve rank remiss). Evidence: peer GENERATION HIT **0.007ms** but mtime-touch remiss re-scanned open+done **~2.03ms**; hub tip had **lost** GENERATION (every call **~3.7ms**); unittest `test_queue_known_keys_content_fp_hit_skips_parse` already required CONTENT_FP but impl missing. Landed hub+peer-1: `QUEUE_KNOWN_KEYS_CONTENT_FP_2026_09_08` + re-port `QUEUE_KNOWN_KEYS_GENERATION` to hub; clear via `clear_rank_opportunities_cache`; EXPECTED md5. Measure: peer remiss_after_touch **2.031→0.049ms** (~**41×**); hub cold **4.3ms** → HIT **0.007ms** → remiss **0.070ms**; CONTENT_FP+GENERATION unittests OK. Metric: **latency**/rank remiss + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L116 SOFT_REFRESH / L65 GENERATION claim. Next: skip post-wait measure if still paying, or wire `queue_md_known_keys` into cold miss parse.

- **2026-09-08 (peer-1 / efficiency_researcher L116)** — Completes L115 Next residual (soft_refresh warm gather when horizon fresh) + Lane E daemon-up theater. Evidence: warm `_soft_refresh_horizon` still paid gather+write on remiss/race while freshest age ≤ **900s** (live soft **~52ms** age≈1.4s) and on every `horizon_stale` heal while improve already up (scan only fires when improve_running). Landed hub+peer-1+hub-protect: `SOFT_REFRESH_DEFER_WARM_GATHER_DAEMON_UP_2026_09_08` + `SOFT_REFRESH_SKIP_WHEN_HORIZON_FRESH_2026_09_08` + shared `HORIZON_STALE_SEC=900`; unittests defer+skip+warm-stale; EXPECTED md5 `cd80b9dd…`. Measure: daemon-up defer med **0.007ms**; fresh skip **0.036ms** gather=**0**; stale+daemon-down still ~**50ms**; live warm **4.5ms** deferred. Metric: **latency**/heal remiss + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L109 SOFT_REFRESH_NO_COLD / L115 ASI_DAEMON_PROBE. Next: QUEUE_KNOWN_KEYS peer←hub or skip post-wait measure if still paying.

- **2026-09-08 (peer-1 / efficiency_researcher L115)** — Completes L114 Next residual (ASI daemon probe wake==TTL). Evidence: `asi_rubric.DAEMON_PROBE_TTL_SEC=30` == `continuous_wake_sec=30` → `_launchctl_running` remissed every heartbeat while heal already floors at **35**. Landed hub+peer-1: `ASI_DAEMON_PROBE_TTL_WAKE_FLOOR_2026_09_08` — `daemon_probe_effective_ttl_sec()` prefers `heal.probe_effective_ttl_sec` else wake+5; wire `_launchctl_running`; unittest `test_daemon_probe_ttl_wake_floor_hits_at_wake_boundary` + TTL remiss uses effective; EXPECTED md5. Measure: age **30.5** HIT calls=**1** (**0.004ms**); eff_ttl=**35**. Metric: **latency**/ASI+write_prompts remiss + Lane E. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. Distinct from L114 PROBE_TTL_WAKE_FLOOR / L96 ASI_SHARE / L100 ASI_PROBE_CTX. Next: soft_refresh warm gather when horizon fresh (~75–108ms).

- **2026-09-08 (peer-1 / efficiency_researcher L114)** — Lane E wake==TTL perpetual scan remiss (ASN noop/compact). Evidence: `_PROBE_TTL_SEC=30` == `continuous_wake_sec=30` → every heartbeat remissed scan/ps/systemd (**~10–33ms**) vs HIT **0.002ms**; same collision as `PRE_DISPATCH_MAINT_WAKE_FLOOR`. Landed hub+peer-1+hub-protect: `PROBE_TTL_WAKE_FLOOR_2026_09_08` — `probe_effective_ttl_sec()` = max(bare, wake+5); wire `_probe_cached` / systemd batch / `_ps_axo_lines` / dual_ns / `scan_bottlenecks`; unittest `test_probe_ttl_wake_floor_hits_at_wake_boundary`; EXPECTED md5. Measure: wake-boundary **10.36→0.009ms** (~**1150×**); effective=**35s**. Metric: **latency**/wake + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L113 PROC_LAZY_RSS / L112 HEAD_GEN / L99 COMPACT_SKIP. Next: ASI `DAEMON_PROBE_TTL_SEC` same wake-floor or soft_refresh warm gather when horizon fresh.

- **2026-09-08 (peer-1 / efficiency_researcher L113)** — Lane E /proc VmRSS theater after L98 snapshot. Evidence: cold `_linux_proc_ps_like_lines` opened ``status``/VmRSS for **all** UID pids (~168; with_rss med **~4.35ms**); ballast needles hit **1**; forever/job_stopped only need cmdline (cmdline-only med **~1.48ms**). Landed hub+peer-1+hub-protect: `PROC_LAZY_RSS_BALLAST_NEEDLES_2026_09_08` — defer VmRSS until args ∈ `_RAM_BALLAST_NEEDLES` (non-needle rss=0); unittest `test_proc_lazy_rss_ballast_needles`; TTL remiss tests age via `probe_effective_ttl_sec()` (eff=35 vs `_PROBE_TTL_SEC`=30 false-MISS); EXPECTED md5. Measure: lazy walk med **~2.62ms** (~**1.7×**); cold scan **~32.6ms** (was **~41–58ms**); ballast RSS live **127024**; status opens **1**. Metric: **latency**/scan + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L112 HEAD_GEN / L110 PS_AXO share / L98 PROC_SNAPSHOT. Next: soft_refresh warm gather when horizon fresh (~75–108ms) or QUEUE_KNOWN_KEYS peer←hub.

- **2026-09-08 (peer-1 / efficiency_researcher L112)** — Completes L110/L111 Next residual (progress_fp git rev-parse). Evidence: after L110 PS_AXO share, warm `progress_fingerprint` still shelled ``git rev-parse HEAD`` every tick (**~2.9–3.9ms**). Landed hub+peer-1+hub-protect-golden: `PROGRESS_FP_HEAD_GEN_CACHE_2026_09_08` — `_progress_head_short` HEAD+index gen-cache; clear via `clear_live_adapt_fp_generation_cache`; unittest `test_progress_fp_head_gen_cache_skips_git`; EXPECTED md5. Measure: miss **1** shell → HIT **0** shells med **~0.08–0.21ms** (~**15–40×**). Metric: **latency**/heal progress_fp + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L111 Darwin / L110 PS_AXO / L108 SCAN_ADAPT_FP_GEN. Next: soft_refresh warm gather when horizon fresh (~75–108ms) or QUEUE_KNOWN_KEYS peer←hub.

- **2026-09-08 (peer-1 / efficiency_researcher L111)** — Completes L109/L110 Next residual (Darwin `_heal_improve_daemon` install_fn cold import). Evidence: Darwin path eagerly `import automation_improve` (~**14–80ms**) before `_kickstart_or_install` even when kickstart succeeds and `install_fn` never runs; timeout also needed `improve.PLIST_PATH`. Landed hub+peer-1+hub-protect: `HEAL_IMPROVE_DARWIN_NO_COLD_IMPROVE_IMPORT_2026_09_08` — early already-active skip; `_lazy_improve_install` imports only when install invoked; flock-busy uses home LaunchAgents plist only; unittest `test_heal_improve_darwin_no_cold_improve_import`; EXPECTED md5. Measure: darwin kickstart-ok **0.517ms** / improve **not** in modules (eager cold baseline **~14–80ms**); install_fn still callable. Linux path unchanged. Metric: **latency**/heal + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L110 PROGRESS_FP / L109 SOFT_REFRESH / L97 IMPROVE_LABEL. Next: progress_fp git rev-parse gen-cache (~2ms) or skip post-wait measure if still paying.

- **2026-09-08 (peer-1 / efficiency_researcher L110)** — Lane E progress_fp heal-tick theater (ASN noop/compact residual adjacent). Evidence: `progress_fingerprint` shelled ``ps -u … args=`` every tick (**med ~27.6ms**) + cold `import peer_transcript` for last_cycle; `_ps_axo_lines` / `_read_state` already in-module. Landed hub+peer-1+hub-protect: `PROGRESS_FP_SHARE_PS_AXO_2026_09_08` + `PROGRESS_FP_NO_TRANSCRIPT_IMPORT_2026_09_08`; unittest `test_progress_fp_share_ps_axo_no_transcript`; EXPECTED md5. Measure: warm fp **~27.6→~2.2ms** (~**12×**; residual git rev-parse); cold **peer_transcript not** in modules; ps -u shells **0**; agents parity 17=17. Metric: **latency**/heal progress_fp + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L98 PROC_SNAPSHOT / L93 PS_AXO_RSS / L109 SOFT_REFRESH. Next: progress_fp git rev-parse gen-cache (~2ms) or Darwin improve install_fn lite.

- **2026-09-08 (peer-1 / efficiency_researcher L109)** — Completes L108 Next residual (soft_refresh / Darwin heal improve import). Evidence: `_soft_refresh_horizon` cold-imported `automation_improve` (~**58ms**) + `gather_signals` (~**196ms**) on every `horizon_stale` heal while improve daemon was already up and wake followed. Landed hub+peer-1+hub-protect: `SOFT_REFRESH_NO_COLD_IMPROVE_IMPORT_2026_09_08` — `sys.modules.get`; deferred when unloaded (wake writes); warm path still gather+write; unittest `test_soft_refresh_no_cold_improve_import`; EXPECTED md5. Measure: cold deferred **0.001ms** / improve **not** in modules (~**250ms→0**); warm still calls gather+write. Darwin `_heal_improve_daemon` import left (install_fn needs improve; Linux returns before import). Metric: **latency**/heal + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L108 SCAN_ADAPT_FP_GEN / L97 IMPROVE_LABEL_NO_IMPORT / L100 ASI_PROBE_CTX. Next: Darwin install_fn lite or remaining cold heal improve sites if Darwin heal matters.

- **2026-09-08 (peer-1 / efficiency_researcher L108c)** — Harden L108 unittest vs dirty-hub index churn. Evidence: hub `test_scan_adapt_fp_gen_cache_skips_git_shells` false-MISS (`hit_n=2`) when live index mtime moved between calls. Pinned `_git_head_index_mtime_ns_for_scan` in test; hub+peer OK. Measure unchanged: HIT shells **0**. Paths: `tests/test_peer_self_heal.py`. Distinct from L108b NameError heal.

- **2026-09-08 (peer-1 / efficiency_researcher L108b)** — Confirm+heal L108 land. Evidence: `clear_scan_bottlenecks_cache` called undefined `clear_live_adapt_git_fingerprint_cache` → **NameError** on invalidate/clear (blocked scan remiss + plan-gate). Fixed: single `clear_live_adapt_fp_generation_cache()`; re-synced hub+hub-protect+EXPECTED md5; unittest `test_scan_adapt_fp_gen_cache_skips_git_shells` OK. Re-measure: miss **12.1ms**/2 shells → HIT med **0.0215ms**/0 (~**560×**). Metric: **latency**/scan + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L108 gen-cache land itself. Next: `_soft_refresh_horizon` / Darwin `_heal_improve_daemon` improve import if cold heal matters.

- **2026-09-08 (peer-1 / efficiency_researcher L108)** — Completes L107 Next residual (adapt fp git shells on scan MISS). Evidence: lite `_live_adapt_git_fingerprint` shelled `rev-parse`+`porcelain` every MISS (**~3.8–9.6ms**, 2 shells) while `automation_adapt` already had `GIT_FP_HEAD_INDEX_GENERATION`. Landed hub+peer-1: `SCAN_ADAPT_FP_GEN_CACHE_2026_09_08` — HEAD+index mtime gen-cache + `clear_live_adapt_fp_generation_cache` (wired into `clear_scan_bottlenecks_cache`); unittest `test_scan_adapt_fp_gen_cache_skips_git_shells`; EXPECTED md5. Measure: miss **3.809ms**/2 shells → gen HIT med **0.023ms**/0 shells (~**165×**); scan TTL-remiss with gen HIT **~1.0ms** git shells **0** (was 2). Metric: **latency**/scan + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from SCAN_ADAPT_NO_COLD_IMPORT / SCAN_POISON / L107 SNAPSHOT. Next: `_soft_refresh_horizon` / Darwin `_heal_improve_daemon` improve import if cold heal matters (unstaged-only porcelain may lag gen until index/HEAD moves — same caveat as adapt).

- **2026-09-08 (peer-1 / efficiency_researcher L107)** — Completes L103/L106 Next residual (factory ops-miss ~80ms). Evidence: `build_team_snapshot_context` cold miss called `compute_factory_progress()` (~**67ms**, dominated by `scan_bottlenecks` ~**36ms**) + `import peer_transcript` for last_cycle. Landed hub+peer-1: `SNAPSHOT_FACTORY_SKIP_SCAN_2026_09_08` (`_factory_progress_for_snapshot` — generation HIT else `bottlenecks=[]`) + `SNAPSHOT_LAST_CYCLE_NO_TRANSCRIPT_2026_09_08` (`_format_last_cycle_block_light` via poison); unittest `test_snapshot_ops_miss_skips_scan_and_transcript`; EXPECTED md5. Measure: miss med **~0.9ms** (first cold imports ~**30ms**); scan calls **0**; `peer_transcript` **not** in modules; warm HIT **~0.4ms**. Metric: **latency**/write_prompts snapshot + Lane E. Paths: `scripts/peer_team_context.py`, `tests/test_peer_team_context.py`. Distinct from L106 HUB_PROTECT_PAUSE / L103 SNAPSHOT_OPS / L104 SCAN_POISON. Next: adapt fp git shells on scan MISS (~11ms) gen-cache.

- **2026-09-08 (peer-1 / efficiency_researcher L106)** — Completes L104/L105 residual (land_hold on clear pause). Evidence: after SCAN_ADAPT/POISON, `_hub_protect_restore_paused` still cold-imported `peer_land_hold` (~**7ms**) when flags/wrap clear. Landed hub+peer-1: `HUB_PROTECT_PAUSE_NO_LAND_HOLD_IMPORT_2026_09_08` — fast-negative flags+wrap before import; unittest `test_hub_protect_pause_no_land_hold_import_when_clear`; EXPECTED md5. Measure: clear path **0.271ms**; land_hold **not** in modules; cold scan imports=`peer_last_cycle_poison` only; 61 OK. Metric: **latency**/scan + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from SCAN_ADAPT / SCAN_POISON / L103 SNAPSHOT_OPS. Next: adapt fp git shells on scan MISS (~11ms) gen-cache, or factory ops-miss (~80ms).


- **2026-09-08 (peer-1 / efficiency_researcher L104)** — Completes L101/L103 Next residual (heal-scan cold imports). Evidence: cold `scan_bottlenecks` compiled `automation_adapt` + `peer_transcript` (~**72ms**). Landed hub+peer-1: `SCAN_ADAPT_NO_COLD_IMPORT_2026_09_08` (`_should_re_adapt_for_scan` lite fp ≡ adapt) + `SCAN_POISON_NO_TRANSCRIPT_IMPORT_2026_09_08` (`peer_last_cycle_poison`); unittests + EXPECTED md5. Measure: cold scan **~72→~19ms** (~**3.8×**); adapt/transcript **not** in `sys.modules`; fp parity True. Metric: **latency**/scan + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L105 HUB_PROTECT_PAUSE / L103 SNAPSHOT_OPS / L102 RANK_MERGE / L101 FACTORY_STATE_SCRUB. Next: `_soft_refresh_horizon` / Darwin `_heal_improve_daemon` improve import if cold heal matters.

- **2026-09-08 (peer-1 / efficiency_researcher L103)** — Lane E snapshot miss theater. Evidence: `format_team_snapshot` JSON miss fell through to full `build_team_context` (~**185–230ms** learning JSONL + diagnose) while snapshot only reads `SNAPSHOT_SECTION_ORDER`. Landed hub+peer-1: `EFFICIENCY_SNAPSHOT_OPS_ONLY_2026_09_08` — `build_team_snapshot_context()` ops-only (format assign, no `write_work_assign_md`); wire miss path; seed-minimal unittests avoid OOM; EXPECTED md5. Measure: miss format cold **~60.6ms** / warm **~0.24ms** vs full **~224ms** (~**3.7×** / **~900×**); `build_team_context` **not** called on miss. Metric: **latency**/write_prompts+dispatch snapshot + Lane E. Paths: `scripts/peer_team_context.py`, `tests/test_peer_team_context.py`. Distinct from EFFICIENCY_SNAPSHOT_JSON_GENERATION / L102 RANK_MERGE / L101 FACTORY_STATE_SCRUB. Next: factory cold remiss on ops miss (~80ms) or heal deferred-poison transcript import.

- **2026-09-08 (peer-1 / efficiency_researcher L102)** — Completes CREATIVE Rank-merge residual. Evidence: `_merge_research_opportunities` cold-imported `peer_dual_research` every rank remiss miss (**~17.7ms**; re-import after ballast **~6ms**) solely to read `dual-research-findings.json`. Landed hub+peer-1: `RANK_MERGE_NO_DUAL_IMPORT_2026_09_08` — `_dual_findings_to_opportunities` disk JSON (parity with `findings_to_opportunities`); unittest `test_merge_research_opportunities_no_dual_import`; EXPECTED md5. Measure: merge **~17.7→~2.4ms**; `peer_dual_research` **not** in `sys.modules`; titles/priorities match. Metric: **latency**/rank remiss + Lane E. Paths: `scripts/automation_improve.py`, `tests/test_automation_improve.py`. Distinct from L101 FACTORY_STATE_SCRUB / L100 ASI_PROBE_CTX / L99 COMPACT_SKIP. Next: heal-scan deferred-poison transcript import (~75ms) or `_soft_refresh_horizon` improve import.

- **2026-09-08 (peer-1 / efficiency_researcher L101)** — Factory meter cold-import theater on state load. Evidence: `_load_state_with_source` / `_scrub_state_dict` cold-imported `peer_transcript` (**~74.8ms**, pulled worktree) just to scrub poison; Linux `_mac_offloaded` also imported `dgx_watch` (**~34.5ms**) when marker absent. Landed hub+peer-1: `FACTORY_STATE_SCRUB_POISON_LIGHT_2026_09_08` (`peer_last_cycle_poison` + `_read_state_json`; meter read-only) + `FACTORY_MAC_OFFLOAD_LINUX_NO_DGX_IMPORT_2026_09_08`; unittest `test_factory_state_scrub_poison_light_no_transcript`; EXPECTED md5. Measure: `_load_state_with_source` **0.60ms** imports=`peer_last_cycle_poison` only; deferred scrub still clears verify_ok; Linux mac_offload **False** with dgx import blocked; dgx_watch **not** in modules after compute. Metric: **latency**/factory cold + Lane E. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L100 ASI_PROBE_CTX / L86 MAC_OFFLOADED_TTL / L84 OPEN_AND_DONE. Next: heal-scan `_is_last_cycle_deferred_poison` still imports transcript (~75ms) on cold scan path; or `_soft_refresh_horizon` improve import.

- **2026-09-08 (peer-1 / efficiency_researcher L100)** — Completes L97 Next residual (ASI `_probe_ctx` string-import theater). Evidence: `asi_rubric._probe_ctx` cold-imported `IMPROVE_LABEL`+`SCRIPTS` (**~11.4ms**) while module already defines `SCRIPTS` and label is `com.togi.{ns}-improve-loop`. Landed hub+peer-1: `ASI_PROBE_CTX_NO_IMPROVE_IMPORT_2026_09_08`; unittest `test_probe_ctx_no_import_automation_improve`; EXPECTED md5. Measure: `_probe_ctx` med **0.187ms** with improve **not** in `sys.modules` (~**61×** vs import **11.44ms**); label matches `IMPROVE_LABEL`. Metric: **latency**/ASI+factory remiss + Lane E. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. Distinct from L99 COMPACT_SKIP_DAEMON_SNAP / L97 IMPROVE_LABEL_NO_IMPORT / L96 ASI_SHARE. Next: `_soft_refresh_horizon` still needs real improve import for gather/write (Darwin `_heal_improve_daemon` install_fn too).

- **2026-09-08 (peer-1 / efficiency_researcher L99)** — Lane E noop/compact theater (ASN parallel_dispatch). Evidence: tests already required `COMPACT_SKIP_DAEMON_SNAP` but `_md_has_open_marker` missing; live WQ stale_daemon/wake/met **open=0** yet compact shelled `daemon_status_snapshot` **2×** (cold snap1 **~10ms**). Landed hub+peer-1: `_md_has_open_marker` + open-gate for stale-daemon / wake / MET-loop+noop; **one** shared snap when needed; needle `COMPACT_SKIP_DAEMON_SNAP_2026_09_08`; unittests 3/3 OK; EXPECTED md5. Measure: live snaps **0**; forced-open snaps **1** (**~4.8ms**); warm compact **~2.6ms**. Metric: **latency**/compact + Lane E. Paths: `scripts/project_automation.py`, `tests/test_automation.py`. Distinct from COMPACT_NOOP_HEAL_TTL / L98 PROC_SNAPSHOT. Next: skip post-wait measure on timeout (~3.5ms) or remaining Darwin-only improve import if cold heal matters.

- **2026-09-08 (peer-1 / efficiency_researcher L98)** — Completes L93/L95 residual (cold `ps -axo` after RSS-share). Evidence: cold scan still shelled ``ps -axo`` **~23–40ms**; `/proc` uid cmdline+VmRSS med **~4.9–6.9ms**. Landed hub+peer-1: `PROC_SNAPSHOT_REPLACE_PS_AXO_2026_09_08`; unittests `test_proc_snapshot_replace_ps_axo_skips_ps_shell` + forever TTL ps-fallback; EXPECTED md5. Measure: cold scan **ps shells=0** (systemctl batch only **~8ms**); `_ps_axo_lines` **~6.9ms**; peer daemon PID live via gitfile. Metric: **latency**/scan. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L97 IMPROVE_LABEL_NO_IMPORT / L93 RSS-share / L96 ASI_SHARE.

- **2026-09-08 (peer-1 / efficiency_researcher L97)** — Completes L96 Next residual (cold first-import compile). Evidence: `dual_namespace_collision` → `_improve_label` → `import automation_improve` paid **~14–19ms** `compile` on every dual-ns miss; scan body also imported for the same string. Landed hub+peer-1: `IMPROVE_LABEL_NO_IMPORT_2026_09_08` — `_improve_label()` uses `com.togi.{ns}-improve-loop` formula; scan body uses `_live_improve_label() or _improve_label()` (no import); unittest `test_improve_label_no_import_automation_improve`; EXPECTED md5. Measure: dual_ns cold **~19→0.79ms**; `automation_improve` **not** in `sys.modules` after dual_ns; 58 unittests OK. Metric: **latency**/scan+dual_ns + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L96 ASI_SHARE / L95 confirm / PROC_SNAPSHOT. Next: remaining heal-path `import automation_improve` (~3 sites) if cold heal matters.

- **2026-09-08 (peer-1 / efficiency_researcher L96)** — Completes creative-backlog residual (Share ASI daemon TTL with heal batch). Evidence: `asi_rubric._systemd_user_active` shelled **2×** per write_prompts/factory remiss (~**16ms**) while heal `SYSTEMD_IS_ACTIVE_BATCH` already covers same units. Landed hub+peer-1: `ASI_SHARE_SYSTEMD_IS_ACTIVE_BATCH_2026_09_08` — prefer `peer_self_heal._systemd_user_active` for batch units; unittest `test_systemd_user_active_shares_heal_batch`; EXPECTED md5. Measure: peer+improve **is_active 2→1** (~**16→11ms**); after heal warm **0** ASI shells. Metric: **latency**/write_prompts+factory. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. Distinct from L95 confirm / L92 SYSTEMD_IS_ACTIVE_BATCH / L91 LOG_HAS. Next: skip cold first-import compile (~40ms) unless remiss demands.

- **2026-09-08 (peer-1 / efficiency_researcher L95)** — Lane E verify after mid-clobber. Evidence: cold `scan_bottlenecks` after cache clear → shells=**2** (systemctl batch **~7.5ms** + `ps -axo pid=,rss=,comm=,args=` **~19ms**); **0×** `ps -u`; remiss body (TTL warm) **~2.0ms**. Restored then dropped broken `_prefetch` NameError (batch via `_systemd_is_active_states` suffices). Unittests `tests/test_peer_self_heal.py` **56 OK**; EXPECTED md5 synced hub+peer. Metric: **latency**/scan cold shell count. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L93/L94 land. Next: cold first-import `compile` theater (~40ms) only if remiss stays >shell.

- **2026-09-08 (peer-1 / efficiency_researcher L93)** — Completes L92 Next residual (cold **2×ps**). Evidence: after systemctl batch, cold still paid ``ps -axo`` + ``ps -u … rss`` (~**80ms**). Landed hub+peer-1: `PS_AXO_RSS_SHARE_BALLAST_2026_09_08` — `_ps_axo_lines` uses ``pid=,rss=,comm=,args=``; `_iter_ram_ballast_pid_rss_args` reuses snapshot (Darwin keeps ``ps -u`` fallback); `LAUNCHCTL_DARWIN_ONLY_2026_09_08` skips launchctl on Linux; unittests + EXPECTED md5. Measure: cold scan **~172→~80ms**; subprocess **7→2** (1×ps + 1×systemctl batch). Metric: **latency**/scan_bottlenecks + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L92 SYSTEMD_IS_ACTIVE_BATCH / L88 PS_AXO TTL. Next: profile non-shell scan body (~50ms residual).

- **2026-09-08 (peer-1 / efficiency_researcher L92)** — Completes L91 Next residual (scan_bottlenecks systemctl fork tax). Evidence: cold scan shelled **5×** `systemctl --user is-active` (~**36ms**); batch argv **~4.6–10ms**. Landed hub+peer-1: `SYSTEMD_IS_ACTIVE_BATCH_2026_09_08` shared unit→state TTL + `clear_systemd_active_cache` (supersedes scan-only PREFETCH); unittest `test_systemd_is_active_batch_ttl`. Measure: systemctl calls **5→1**. Metric: **latency**. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L91 LOG_HAS_RECENT / L88 PS_AXO. Next: **2×ps** axo+rss ~80ms → L93.

- **2026-09-08 (peer-1 / efficiency_researcher L94)** — Confirm L93 needles live + verify. Evidence: cold `_scan_bottlenecks_body` after clear caches → shells=**2** (systemctl batch **9.0ms** + `ps -axo pid=,rss=,comm=,args=` **40.7ms**); **0×** `ps -u`; **0×** launchctl. Unittests: `test_ps_axo_rss_share_ballast_no_second_ps` + `test_launchctl_darwin_only_skips_on_linux` + `test_systemd_is_active_batch_ttl` OK; hub+peer EXPECTED md5 synced. Metric: **latency**/scan cold shell count. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L93 land / L92 prefetch. Next: cold non-shell residual (~50ms adapt/log tails).
- **2026-09-08 (peer-1 / efficiency_researcher L93)** — Completes L92 Next residual (cold **2×ps** + Linux launchctl theater) and replaces duplicate PREFETCH with impl-path batch. Evidence: after L92 prefetch, cold still paid `ps -axo` + `ps -u … rss` (~**31ms** each) and **3×** `launchctl` for ram-peer on DGX; prefetch duplicated `SYSTEMD_IS_ACTIVE_BATCH`. Landed hub+peer-1: keep `SYSTEMD_IS_ACTIVE_BATCH_2026_09_08`; drop prefetch; `PS_AXO_RSS_SHARE_BALLAST_2026_09_08` (`pid=,rss=,comm=,args=` + `_parse_ps_axo_pid_rss_args` / `_iter_ram_ballast_pid_rss_args`); `LAUNCHCTL_DARWIN_ONLY_2026_09_08`; unittests `test_systemd_is_active_batch_ttl` + `test_ps_axo_rss_share_ballast_no_second_ps` + `test_launchctl_darwin_only_skips_linux`; EXPECTED md5. Measure: is-active **1**; ps_axo **1**; ps_u **0**; launchctl **0**; cold ~**51–112ms** (was ~150–215ms). Metric: **latency**/scan cold + Lane E heal-scan theater. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L92 PREFETCH / L91 LOG_HAS / L88 PS_AXO TTL. Next: cold residual adapt fingerprint / log tails (~50ms) if remiss.

- **2026-09-08 (peer-1 / efficiency_researcher L93)** — Completes L92 Next residual (cold **2×ps**). Evidence: after systemctl batch, cold still paid ``ps -axo`` + ``ps -u … rss`` (~**80ms**). Landed hub+peer-1: `PS_AXO_RSS_SHARE_BALLAST_2026_09_08` — `_ps_axo_lines` uses ``pid=,rss=,comm=,args=``; `_iter_ram_ballast_pid_rss_args` reuses snapshot (Darwin keeps ``ps -u`` fallback); `LAUNCHCTL_DARWIN_ONLY_2026_09_08` skips launchctl on Linux; unittests + EXPECTED md5. Measure: cold scan **~172→~80ms**; subprocess **7→2** (1×ps + 1×systemctl batch). Metric: **latency**/scan_bottlenecks + Lane E. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L92 SYSTEMD_IS_ACTIVE_BATCH / L88 PS_AXO TTL. Next: profile non-shell scan body (~50ms residual).

- **2026-09-08 (peer-1 / efficiency_researcher L92)** — Completes L91 Next residual (scan_bottlenecks systemctl fork tax). Evidence: cold scan shelled **5×** `systemctl --user is-active` (~**36ms**); batch argv **~4.6–10ms**. Landed hub+peer-1: `SYSTEMD_IS_ACTIVE_BATCH_2026_09_08` shared unit→state TTL + `clear_systemd_active_cache` (supersedes scan-only PREFETCH); unittest `test_systemd_is_active_batch_ttl`. Measure: systemctl calls **5→1**. Metric: **latency**. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L91 LOG_HAS_RECENT / L88 PS_AXO. Next: **2×ps** axo+rss ~80ms → L93.


- **2026-09-08 (peer-1 / efficiency_researcher L91)** — Completes L90 Next residual (`_log_has_recent_any` factory remiss). Evidence: live improve-loop.log **~50MB**; remiss `_self_sufficiency_score` re-sought 32KB tail every tick (**MISS ~0.17–0.55ms**; profile `str.lower` top remiss tottime). Landed hub+peer-1: `LOG_HAS_RECENT_ANY_MTIME_MEMO_2026_09_08` path+mtime_ns+needles memo + `clear_log_has_recent_any_cache`; age string stays fresh; unittest `test_log_has_recent_any_mtime_memo_skips_reread`; EXPECTED md5. Measure: HIT med **0.0025ms** (~**219×** vs MISS **0.553ms**); second call tail reads **0**. Metric: **latency**/factory remiss + Lane E wake-credit theater. Paths: `scripts/asi_rubric.py`, `tests/test_asi_rubric.py`. Distinct from L90 Path.home / L89 BOUNDED / L75 LOG_TAIL_SEEK. Next: scan_bottlenecks cold ~**149ms** / ~10 subprocess (systemctl).

- **2026-09-08 (peer-1 / efficiency_researcher L90)** — Completes L88/L89 STRSET+BOUNDED correctness. Evidence: STRSET body used `os.path.expanduser("~")` so `Path.home` mock ignored → `test_improve_log_path_prefers_freshest_peer_ns` FAIL (chose live hub log). Landed hub+peer-1: `os.fspath(Path.home() / ".config")` + `statmod.S_ISREG`; 33 unittests OK; EXPECTED md5. Measure: mock home prefers peer-6 **True**; Path.__eq__ **0**; cold **0.118ms**. Metric: **latency**/correctness. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L89 BOUNDED body / L88 STRSET / L88 ps-axo. Next: `_log_has_recent_any` remiss (~0.17ms).

- **2026-09-08 (peer-1 / efficiency_researcher L89)** — Completes L87 Next residual (improve-log miss candidate shrink). Evidence: after CANDIDATE_STRSET, cold miss still `os.scandir` ~**2240** `~/.config` dirents (**~1.6ms**, 2244× `is_dir`) for **4** live improve logs. Landed hub+peer-1: `IMPROVE_LOG_PATH_BOUNDED_PEERS_2026_09_08` — fallback NS + `peer-0..max_parallel_peers-1` (floor 8) + CONFIG_DIR ns; no scandir; unittest `test_improve_log_path_bounded_peers_no_scandir`. Measure: miss **~1.6ms → ~0.025ms** (~**64×**); profile scandir/is_dir/__eq__=0. Metric: **latency**. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L88 PS_AXO / L87 clear-keep. Next: batch `ps -o stat=` in `_job_stopped` or `_log_has_recent_any` remiss.

- **2026-09-08 (peer-1 / efficiency_researcher L88)** — Completes L87 Next (`Path` candidate shrink / Path.__eq__ storm). Evidence: cold `_improve_log_path` paid **Path.__eq__ ×1384** (~53 dirs, mean **1.172ms**) then full `~/.config` scandir. Landed hub+peer-1: `IMPROVE_LOG_PATH_CANDIDATE_STRSET_2026_09_08` + `IMPROVE_LOG_PATH_BOUNDED_PEERS_2026_09_08` (peer-0..N + fallback NS only); unittests `test_improve_log_path_candidate_strset_no_path_eq` + `test_improve_log_path_bounded_peers_no_scandir`; EXPECTED md5. Measure: Path.__eq__ **0**; scandir **0**; cold **0.154ms** (~**7.6×** vs 1.172ms). Metric: **latency**/Lane E. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L87 FOREVER_PYTHON_PS / L85 REGISTRY. Next: `_log_has_recent_any` remiss or scan cold.

- **2026-09-08 (peer-1 / efficiency_researcher L88)** — Completes L86 Next residual (scan_bottlenecks / job_stopped `ps` theater). Evidence: `_job_stopped_loop_python_pids` called `_forever_python_pid` **4×**; each shelled `ps -axo` (~**14.6ms**); probed **4×ps=56.8ms**; `_job_stopped` ~**148ms** of ~**272ms** scan body. Landed hub+peer-1: `FOREVER_PYTHON_PS_AXO_TTL_2026_09_08` shared `_ps_axo_lines` TTL (=`_PROBE_TTL_SEC`) + `clear_ps_axo_cache` on invalidate; dashboard PID reuses same snapshot; unittest `test_forever_python_ps_axo_ttl_hit`. Measure: 4 forever lookups **axo=1** (was 4). Metric: **latency**/scan_bottlenecks + Lane E heal-scan theater. Paths: `scripts/peer_self_heal.py`, `tests/test_peer_self_heal.py`. Distinct from L87 factory clear port / L86 MAC_OFFLOADED / COMPACT_NOOP_HEAL. Next: batch `ps -o stat=` in `_job_stopped` (still ~18ms HIT tax).

- **2026-09-08 (peer-1 / efficiency_researcher L87)** — Completes L86 Next + Lane E remiss theater: peer-1 was missing hub L85. Evidence: `clear_factory_progress_cache` still called `clear_improve_log_path_cache` → remiss `_improve_log_path` **~2.0ms** scandir (53 cands); `_registry_stats` re-parsed JSON every natural remiss. Ported hub: `REGISTRY_STATS_MTIME_MEMO_2026_09_08` + `FACTORY_CLEAR_KEEP_IMPROVE_LOG_TTL_2026_09_08`; unittest `test_registry_stats_mtime_memo_hit`. Measure: improve_log after clear_factory **~2.0ms → 0.0008ms** (~**2500×**); remiss tick **~3.0ms → ~0.5ms**; registry JSON loads **1** on double-call. Metric: **latency**. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L86 MAC_OFFLOADED (already on peer-1). Next: `_self_sufficiency_score` / `_log_has_recent_any` remiss (~0.25ms) or Path candidate list shrink on improve-log miss.

- **2026-09-08 (peer-1 / efficiency_researcher L86)** — Completes L84/L85 Next (`_mac_offloaded` sticky marker remiss). Evidence: factory MISS called `_mac_offloaded` **5×**; first MISS **2.24ms** (dgx_watch import). Landed hub+peer-1: `MAC_OFFLOADED_TTL_2026_09_08` 30s memo + `clear_mac_offloaded_cache`; unittest `test_mac_offloaded_ttl_hit`. Measure: HIT **0.00019ms** (~**11800×**). Metric: **latency**. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L85 REGISTRY_STATS / L84 OPEN_AND_DONE. Next: pick remiss probe.

- **2026-09-08 (peer-1 / efficiency_researcher L85)** — Completes L84 Next residual (`_improve_log_path` / remiss JSON). Evidence: natural WQ remiss still re-read `repos/registry.json` every tick (probe miss **0.074ms**; 5 remiss reads=5 before); `clear_factory_progress_cache` also dropped L75 improve-log path TTL (scandir remiss theater in tests/forced clear). Landed hub: `REGISTRY_STATS_MTIME_MEMO_2026_09_08` + `FACTORY_CLEAR_KEEP_IMPROVE_LOG_TTL_2026_09_08` (clear_factory no longer calls `clear_improve_log_path_cache`); unittest `test_registry_stats_mtime_memo_hit`; EXPECTED md5. Measure: registry HIT **0.003ms** (~**25×**); 5 WQ remiss registry reads **0**; clear_factory scandir delta **0**. Metric: **latency**/factory remiss + Lane E registry/log theater broken. Paths: `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L84 OPEN_AND_DONE / L83 packed-refs / L75 improve-log scandir. Next: `_mac_offloaded` JSON remiss or pathlib Path.__eq__ storm on improve-log candidate list.

- **2026-09-08 (peer-1 / efficiency_researcher L84)** — Completes L80/L83 Next residual (`_open_and_done_items` index share). Evidence: WQ-touch factory remiss dominated by `all_open` + Done-section walk (~0.1–0.5ms) after `open_work_items` already primed `_queue_md_index`. Landed hub+peer-1: `_QueueMdIndex.backlog_open_items`+`done_items`; wire `all_open` + `_open_and_done_items`; WQ+context mtime memo + `clear_open_and_done_items_cache`; unittest `test_open_and_done_index_share_mtime_hit`; EXPECTED md5. Measure: index-warm mtime-miss **0.066ms** vs old walk **0.092ms**; mtime HIT **0.002ms** (~**40×**); open_and_done dropped out of remiss top profile. Metric: **latency**/factory remiss + Lane E open+done theater broken. Paths: `scripts/project_automation.py`, `scripts/factory_progress.py`, `tests/test_factory_progress.py`. Distinct from L83 packed-refs / L82 transcript shim / L81 DUAL_NS. Next: `_improve_log_path` Path.__eq__ candidate storm on remiss (~pathlib eq ×6k) or `_mac_offloaded` JSON remiss.
